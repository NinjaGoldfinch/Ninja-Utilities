package com.ninjagoldfinch.nz.ninja_utils

import net.fabricmc.api.ModInitializer

object NinjaUtils : ModInitializer {
    const val MOD_ID = "ninja_utils"

    override fun onInitialize() {
        // Common init — this mod is client-only, so nothing here
    }
}
